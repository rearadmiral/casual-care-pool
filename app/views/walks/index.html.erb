<p id="notice"><%= notice %></p>

<h1>Listing Walks</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Requested at</th>
      <th>Finished at</th>
      <th>Dog</th>
      <th>Walker</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @walks.each do |walk| %>
      <tr>
        <td><%= walk.name %></td>
        <td><%= walk.requested_at %></td>
        <td><%= walk.finished_at %></td>
        <td><%= walk.dog_id %></td>
        <td><%= walk.walker_id %></td>
        <td><%= link_to 'Show', walk %></td>
        <td><%= link_to 'Edit', edit_walk_path(walk) %></td>
        <td><%= link_to 'Destroy', walk, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Walk', new_walk_path %>
